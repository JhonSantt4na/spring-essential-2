version: '3.3'
services:
  db:
    image: mysql
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root 
    ports:
      - "3307:3306"
    volumes:
      - santt4na_data:/var/lib/mysql

volumes:
  santt4na_data:
